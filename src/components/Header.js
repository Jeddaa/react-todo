const Header = () => (
  <header>
    <h1 className="todo-header">Todos</h1>
    <p className="todo-headerp">Items will persist in the browser local storage</p>
  </header>
);

export default Header;
